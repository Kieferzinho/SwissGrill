---
const { color = "green", url = "", newtab = false, As = "a", ...props } = Astro.props;
// Cores disponíveis: green, red, yellow, yellow-gold, black, gray-dark
---

<As
	class={`not-prose button button-${color}`}
	href={url}
	target={newtab ? "_blank" : "_self"}
	rel={newtab ? "noopener noreferrer" : ""}
	{...props}
>
	<slot />
</As>
